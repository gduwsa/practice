#include<stdio.h>

int main()

{
	int ch = 0;
	int ret = 0;
	char password[20] = { 0 };
	printf("输入密码:>");
	scanf("%s", password);
	printf("请确认(Y/N):>");
	while((ch = getchar())!='\n')//要让它循环接收读取
	{
		;//空行
	}
	ret = getchar();
	if (ret == 'Y')
	{
		printf("确认成功\n");
	}
	else
	{
		printf("放弃确认\n");
	}
	return 0;
}
